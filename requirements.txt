# pyali runtime + test dependencies
numpy>=1.24
scipy>=1.10
scikit-image>=0.21
scikit-learn>=1.3
h5py>=3.8           # reading/writing .mat (HDF5) files; a pure-python fallback exists
matplotlib>=3.9     # static result figures
plotly==5.24.1      # interactive cell-explorer HTML (pinned; embeds plotly.js for offline use).
                    #   optional: without it the explorer falls back to loading plotly.js from CDN.
pytest>=7.0         # tests only
