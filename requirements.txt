# Dedicated environment for the pyali VOLTAGE-extraction + REGISTRATION modules.
# Python 3.9.7. Fully pinned so the complete env is reproducible from scratch:
#   python3.9 -m venv .venv && . .venv/bin/activate
#   pip install -U pip && pip install -r requirements.txt
#
# --- shared / voltage extraction (pyali core) ---
numpy==2.0.2
scipy==1.13.1
scikit-image==0.24.0
scikit-learn==1.6.1
h5py==3.14.0            # read/write .mat (HDF5 v7.3): output_data.mat stage/device, ALI results
matplotlib==3.9.4       # static result figures
plotly==5.24.1          # interactive cell-explorer HTML (embeds plotly.js offline)
#
# --- registration module (adds these) ---
tifffile==2024.8.30     # SBS JF608 anchor tiles + reconciled cell masks
pandas==2.3.3           # sbs_info / cells / metadata parquets; matched-cell tables
pyarrow==21.0.0         # parquet engine for pandas
#
# --- tests ---
pytest==8.4.2
